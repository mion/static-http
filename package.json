{
  "name": "static-http",
  "version": "2.0.0",
  "description": "Quickly create an HTTP server from a folder to test stuff on 'localhost'. Only 3 dependencies, uses Express.js under the hood.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "static-http": "server.js"
  },
  "keywords": [
    "http",
    "simple",
    "serve",
    "server",
    "static",
    "local"
  ],
  "author": "Gabriel Vieira",
  "license": "MIT",
  "dependencies": {
    "colors": "^1.4.0",
    "commander": "^8.1.0",
    "express": "^4.17.1"
  }
}
